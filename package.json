{
    "name": "tuum-prism-monorepo",
    "private": true,
    "scripts": {
        "build": "turbo run build",
        "dev": "turbo run dev",
        "lint": "turbo run lint",
        "test": "turbo run test",
        "format": "prettier --write \"**/*.{ts,tsx,md,json,yml}\""
    },
    "devDependencies": {
        "prettier": "^3.7.4",
        "turbo": "^2.7.3"
    },
    "packageManager": "pnpm@9.15.0",
    "engines": {
        "node": ">=20.0.0"
    }
}